# Local set guide 
vagrant init ubuntu/bionic64
vagrant up 
vagrant ssh 

# install docker ffing docker docs guide then check the docker version installed
# </> docker --version 
# give vagrant user docker command privileges, adding vagrant user into docker groups 
# </> sudo usermod -aG docker vagrant 
